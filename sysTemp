#! /bin/bash

# A simple system temperature module

temp=$( sensors | grep Tdie | awk '{print $2}' | cut -d'+' -f2-)
tempRaw=${temp%*°C}
red="#b6465f"
gold="#e89709"

if [ `bc <<< "$tempRaw < 50"` ]; then
	echo $tempRaw°C && echo && echo "#e89709"
else
	echo $tempRaw°C && echo && echo "#ff0000"
fi
